local   all             {surole}                                peer           map=test
local   all             all                                     {auth_local}
host    all             all             127.0.0.1/32            {auth_host}
local   replication     {replrole}                              {auth_local}
