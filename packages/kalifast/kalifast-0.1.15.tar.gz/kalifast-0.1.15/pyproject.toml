[project]
name = "kalifast"
description = "Python package for kalifast record npp plugin"
version = "0.1.15"
authors = [
    { name = "EISGE", email = "e@eisge.com" }
]
dependencies = [

]


[build-system]
build-backend = "flit_core.buildapi"
requires = [
        "flit_core >=3.2,<4",        
        "wheel",   
        "setuptools",   
]