from setuptools import setup, find_packages

setup(
    name         = 'jas_nester',
    author       = 'Jjhar Singh',
    author_email = 'sage_r3vok@icloud.com',
    version      = '1.2.0',
    description  = """This function takes a positional argument called “the_list",
    which is any Python list (of, possibly, nested lists).
    Each data item in the provided list is (recursively)
    printed to the screen on its own line."""
    
    )
