#!/usr/bin/env python

import os
import re

from fire import Fire
from traxix.utils import _f


def fp(p, *args, e=None):
    if e:
        args = list(args)
        args.append(f"{e}$")
    for path in _f(*args, p=p):
        print(path)


if __name__ == "__main__":
    Fire(fp)
