[tool.poetry]
name = "sane-python"
version = "0.1.0"
description = "Simple encoding of numeric arrays"
authors = ["Viktor Kronvall <viktor.kronvall@gmail.com>"]
license = "BSD3"

[tool.poetry.scripts]
cat_array = "sane_python.io:cat_array"

[tool.poetry.dependencies]
python = "^3.9"
numpy = "^1.22.3"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
