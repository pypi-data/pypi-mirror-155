from setuptools import setup, find_packages

setup(
    name ='jhfunnycode',
    version = '1.1.0',
    description = 'life is fun',
    author = 'i_enable',
    author_email = None,
    url = None,
    install_requires = [],
    packages=find_packages(),
)
