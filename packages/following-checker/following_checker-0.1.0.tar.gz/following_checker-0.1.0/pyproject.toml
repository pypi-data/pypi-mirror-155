[tool.poetry]
name = "following_checker"
version = "0.1.0"
description = ""
authors = ["LeieXoyo <gen@gen.li>"]

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.27.1"
PySocks = "^1.7.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
mypy = "^0.950"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
